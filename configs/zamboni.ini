[monolith]
timeout = 10

[source:zamboni]
use = aggregator.plugins.zamboni.APIReader
keys = app.install,foo.bar
endpoint = http://localhost:8000/api/monolith/data
oauth_key = blah
oauth_secret = blah

[key:foobar]
fields = foo,bar,baz
groupers = foo,bar

[target:file]
use = aggregator.plugins.files.FileWriter
filename = toto.test
